package INTERVIEWquestions;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class StreamApiPracticeQuestion {

    public static void main(String[] args) {
//        List<Integer> list = Arrays.asList(15,22,8,40,12,35,50,5);
//        //1. find sum of the 3 largest number
//        list.stream().sorted((a,b)->b-a).limit(3).reduce((a,b)->a+b)
//                .ifPresent(System.out::println);

        //2.  find the second smallest number
//               List<Integer> list = Arrays.asList(15,22,8,40,12,35,50,5);
//               list.stream().sorted().skip(1).limit(1).forEach(System.out::println);

        //3.  find the sum of square of even number greator than 10
        List<Integer> list = Arrays.asList(15,22,8,40,12,35,50,5);

    }
}
